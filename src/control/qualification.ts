import type { CapabilityScore,ModelCandidate,ModelRecipe } from "./types.js";
export interface TrialResult{recipeId:string;capability:string;passed:boolean;quality:number;latencyMs:number;substitutions:number;}
export function aggregateCandidate(recipe:ModelRecipe,results:TrialResult[],benchmarkSuite:string,holdoutSuite:string):ModelCandidate{const mine=results.filter(r=>r.recipeId===recipe.id),groups=new Map<string,TrialResult[]>();for(const r of mine)groups.set(r.capability,[...(groups.get(r.capability)??[]),r]);const capabilities:CapabilityScore[]=[...groups.entries()].map(([capability,trials])=>({capability,score:trials.reduce((s,t)=>s+(t.passed?t.quality:0),0)/Math.max(trials.length,1),sampleSize:trials.length}));return{recipe,stage:"benchmarking",capabilities,substitutions:mine.reduce((s,t)=>s+t.substitutions,0),completions:mine.filter(t=>t.passed).length,benchmarkSuite,holdoutSuite};}
export function shouldPromote(challenger:ModelCandidate,champion:ModelCandidate,capability:string){const cs=challenger.capabilities.find(x=>x.capability===capability),bs=champion.capabilities.find(x=>x.capability===capability);return(cs?.sampleSize??0)>=5&&(cs?.score??0)>(bs?.score??0)&&challenger.substitutions<=champion.substitutions;}
