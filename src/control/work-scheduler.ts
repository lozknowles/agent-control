import{CapabilityResolver,type Resource}from'./capabilities.js';import{WorkQueue,scoreResourceForWork,type ResourceLoad,type WorkItem}from'./work-queue.js';export interface Allocation{work:WorkItem;resource:Resource;score:number;}export class WorkScheduler{constructor(readonly queue:WorkQueue,readonly resolver=new CapabilityResolver()){}allocate(resources:Resource[],loads:ResourceLoad[],now=new Date()):Allocation|null{for(const work of this.queue.ready(now)){const eligible=this.resolver.resolve(work.capabilities,resources).resources.map(resource=>({resource,score:scoreResourceForWork(work,resource,loads.find(l=>l.resourceId===resource.id))})).filter(x=>Number.isFinite(x.score)).sort((a,b)=>b.score-a.score);const best=eligible[0];if(best){this.queue.claim(work.id,best.resource.id);return{work,resource:best.resource,score:best.score};}}return null;}preemptFor(incoming:WorkItem,running:WorkItem[]){if(incoming.class!=='interactive'&&incoming.class!=='priority')return[];const victims=running.filter(w=>w.preemptible&&(w.class==='background'||w.class==='batch'));for(const v of victims)this.queue.checkpoint(v.id,{...(v.checkpoint??{at:new Date().toISOString()}),at:new Date().toISOString()});return victims.map(v=>v.id);}}
