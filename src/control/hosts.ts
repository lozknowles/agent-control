export type HostKind='core'|'linux'|'windows'|'android'|'remote-api';export type HostHealth='unknown'|'healthy'|'degraded'|'offline';export interface ExecutionHost{id:string;name:string;kind:HostKind;os:string;transport:'local'|'tailscale-ssh'|'api';core:boolean;capabilities:string[];gpu?:{name:string;vramGb:number};harnesses:string[];}export interface HostState{hostId:string;health:HostHealth;detail?:string;checkedAt:string;}export class ExecutionHostRegistry{private hosts=new Map<string,ExecutionHost>();private states=new Map<string,HostState>();register(h:ExecutionHost){this.hosts.set(h.id,h);this.states.set(h.id,{hostId:h.id,health:'unknown',checkedAt:new Date(0).toISOString()});return h;}get(id:string){return this.hosts.get(id);}list(){return[...this.hosts.values()];}setHealth(hostId:string,health:HostHealth,detail?:string){if(!this.hosts.has(hostId))throw new Error(`host ${hostId} not registered`);const s={hostId,health,detail,checkedAt:new Date().toISOString()};this.states.set(hostId,s);return s;}health(id:string){return this.states.get(id);}core(){return this.list().find(h=>h.core);}canRun(hostId:string,harness:string){const h=this.hosts.get(hostId),s=this.states.get(hostId);return Boolean(h&&s?.health==='healthy'&&h.harnesses.includes(harness));}}
export const builtinHosts:ExecutionHost[]=[{id:'hpubuntu',name:'hpubuntu',kind:'core',os:'Ubuntu Linux',transport:'local',core:true,gpu:{name:'Quadro P5000',vramGb:16},capabilities:['control-plane','local-models','repositories','benchmarks','codex'],harnesses:['codex','llama.cpp']},{id:'msi',name:'MSI',kind:'windows',os:'Windows 11',transport:'tailscale-ssh',core:false,capabilities:['windows','edge','chatgpt-window','codex'],harnesses:['codex','chatgpt-window']},{id:'sentinel',name:'sentinel',kind:'linux',os:'Ubuntu Linux',transport:'tailscale-ssh',core:false,gpu:{name:'Quadro P3000',vramGb:6},capabilities:['resident-small-model','monitoring','failover','codex'],harnesses:['codex','llama.cpp']},{id:'pixel',name:'Pixel',kind:'android',os:'Android',transport:'tailscale-ssh',core:false,capabilities:['mobile','codex','edge-agent'],harnesses:['codex']},{id:'remote-api',name:'Remote API',kind:'remote-api',os:'remote',transport:'api',core:false,capabilities:['cloud-models','burst-capacity'],harnesses:['responses-api']}];
