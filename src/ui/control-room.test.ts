import assert from'node:assert/strict';import test from'node:test';import{controlRoomView}from'./control-room.js';import type{WorkQueueMetrics}from'../control/work-observability.js';const metrics:WorkQueueMetrics={total:12,ready:7,humanReview:2,retrying:1,oldestQueuedAgeMs:7200000,byClass:{interactive:1,priority:2,background:3,batch:6},byStatus:{queued:7,blocked:0,claimed:1,running:1,checkpointed:0,completed:1,failed:0,'human-review':2},batches:[{key:'photos',items:6}],resources:[{id:'sentinel',busy:.25,capacity:1,utilisation:.25}],throughputPerHour:20,estimatedDrainMs:1800000};test('control room view exposes queue and Pixel degraded state',()=>{const v=controlRoomView(metrics,{state:'node-degraded',detail:'ssh ready; pixel node unavailable',recovered:false});assert.match(v.queue,/READY 7/);assert.match(v.queue,/photos/);assert.match(v.queue,/Drain 30m/);assert.match(v.resources,/NODE-DEGRADED/);assert.match(v.resources,/sentinel\s+25% busy/);});test('control room marks recovered Pixel',()=>{assert.match(controlRoomView(metrics,{state:'capability-ready',detail:'recovered',recovered:true}).resources,/RECOVERED/);});
