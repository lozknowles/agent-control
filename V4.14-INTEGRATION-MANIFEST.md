# Agent Control v4.14 integration manifest

Status: EXPERIMENTAL RC1; bounded qualification completed with stated remaining limits. This manifest does not qualify a release.

## Baseline

- Public v4.13.0 source and `origin/main`: `2308dca2de5d4d6c0b099120d47eb11f5131437e`.
- Integration branch: `integration/v4.14-rc-20260923`, in an isolated worktree.
- Candidate package/runtime version: `4.14.0-rc.1`; no v4.14.0 tag exists.
- The local branch named `main` in the shared repository is stale; no change to it, public main, or v4.13.0 was made.

## Selective research provenance

| Research commit | Brought into integration worktree | Deliberately excluded from the candidate source |
| --- | --- | --- |
| `900cb920c8c83bab439b9fafdff6337f3d235724` | Structured native tool-intent translation; bounded deterministic parser/repair; semantic event ledger; numbered-display guard in mutation workspace; frozen solvability assessor; corresponding tests and fixtures. | Strands adapter, Docker research setup, model proxy, fairness scripts, corpus replay/measurement tools, research video recorder. |
| `b2dca0e0f7069205d5bc6cff39037ea5334039bd` | Opt-in no-progress detector and structured-loop observation hooks; focused tests. | AutomationBench containers, reference adapter and benchmark-specific physical-control/recorder scripts. |
| `7171b85859eff98ddb1a62d0f27ab1d1bfe7ab9a` | Run-ID-derived Job identity styling and attribution in Activity, Jobs, Factory, Evidence and browser recording; dashboard and Factory tests. | Original demonstration script is used only for qualification and is not proposed as release code. |

No research branch was merged wholesale. The compatibility path remains the existing bounded JSON tool loop when native semantic tools are not explicitly configured. Raw forensic response capture remains restricted to the isolated synthetic-fixture research vault; it is not enabled for ordinary users.

One integration-only test fix selects distinct ephemeral target and protected-health ports for the experimental model-server fixture. This responds to an observed one-off port collision in a full run; it changes no production model-server behaviour.

## Integration decisions requiring qualification

- The semantic interface and no-progress detector are opt-in APIs of `StructuredChatLoopProvider`; source search has not found production Job admission wiring. They cannot yet be described as ordinary Job capabilities.
- The numbered-display guard is confined to `MutationWorkspace.write` and exact replacement; generic source-write protection across arbitrary harnesses has not been demonstrated.
- The solvability assessor is frozen to five tasks, so the requested generic configurable preflight is not yet established.
- The identity palette has twelve slots and can still collide. Run IDs and text badges remain authoritative; visual colour alone does not identify a Job.
- Multi-file edits are sequential. No transactional or rollback mechanism has been demonstrated.
- The earlier NO_PROGRESS_V1 research run did not causally establish token or wall-time savings, and its frozen corpus is too small to establish a general false-positive rate.

## Release boundary

These files are in a research integration worktree for qualification. Inclusion in a distributable v4.14 package requires individual feature decisions, security and physical gates, clean installation, upgrade, rollback and a final claim audit. No release action is authorised by this manifest.

## RC1 correction provenance

- `e566101d93abcb5198b164cdcd3302c8c572c233`: optional executable/service spawn handling, structured GPU states, prerequisite doctor, installation instructions and regressions.
- `e86718b1a214390357ba845a168f88859de53b19`: keep the doctor core transport-neutral.
- `c6ab22c7b504454f19408d21fc2eddbabeef7071`: reject duplicate keys inside stringified native arguments before repair.
- `79a003160090459659901709ca744414a4bc82ab`: reject duplicate semantic native arguments and the entire ambiguous batch before dispatch. Exact model/clean-install/installed upgrade runtime.
- `c8d132eb5803512726b8c2b18f9e210388870e64`: encode crew activity class metadata with a red/green renderer regression.
- `d16cf168e1b6d5208c0c520352e1f7d275b4a9bd`: encode cache state classes. Final implementation freeze for controlled fault recording and fresh security containment.

Only two dashboard JavaScript files and one renderer test differ between 79a and d16. Full service configuration/admission wiring for optional semantic execution was not added. The separate qualification driver registers generic actions and is retained as evidence, not added as a product benchmark adapter. Experimental feature classifications and all physical revision boundaries are in V4.14-QUALIFICATION-REPORT.md.

The final external sealed manifest binds the documentation/source commit, archive hashes, exact-commit check, videos and all attempts. It does not overwrite rc.0 evidence or promote experimental features.