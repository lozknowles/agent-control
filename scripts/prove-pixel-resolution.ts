import{CapabilityResolver}from'../src/control/capabilities.js';import{fetchNodeResource,runNodeJob}from'../src/control/node-client.js';const token=process.env.AGENT_CONTROL_NODE_TOKEN;if(!token)throw new Error('AGENT_CONTROL_NODE_TOKEN is required');const baseUrl=process.env.PIXEL_NODE_URL??'http://127.0.0.1:18788';const pixel=await fetchNodeResource({baseUrl,token});const request={requires:[{id:'platform.android'},{id:'device.physical'},{id:'harness.codex'},{id:'observe.android.logcat'}]};const resolution=new CapabilityResolver().resolve(request,[pixel]);if(resolution.missing.length)throw new Error(`PIXEL-RESOLUTION-FAIL missing=${resolution.missing.join(',')}`);const job=await runNodeJob<any>({baseUrl,token},'android.observe.logs',{lines:5});if(job.status!=='completed'||job.resource!=='pixel')throw new Error('PIXEL-JOB-FAIL');console.log(JSON.stringify({sentinel:'AGENT-CONTROL-PIXEL-RESOLUTION-PASS',request,resolution:{resources:resolution.resources.map(r=>r.id),satisfied:resolution.satisfied,missing:resolution.missing},job:{status:job.status,resource:job.resource,device:job.device}},null,2));
