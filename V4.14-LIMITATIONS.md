# v4.14 RC1 limitations

Overall status: EXPERIMENTAL. Production remains v4.13.0. See the qualification report and sealed receipts for exact commits.

- v4.13 diagnostic coverage remains **PARTIAL**. The small, correlated **AI-reviewed 47-case** held-out evaluation is not broadened or combined with the prior 264-event research reconciliation or new 56-pair lifecycle check.
- **Pixel physical identity remains unverified.** Fresh multi-host evidence covers two Linux hosts only.
- SEMANTIC_TOOL_V1 remains opt-in and EXPERIMENTAL_NOT_RELEASED. Four of five internal frozen tasks passed. The custom governed qualification runtime did not configure the complete service RuntimeSafetySupervisor. Ordinary configured service integration and independent external generalisation remain open.
- Safe repair had zero false repairs in 150 frozen synthetic/adversarial cases. This does not establish arbitrary tool/model safety. Type/enum coercion, ambiguous keys and permission expansion remain rejected.
- Numbered-output protection is confined to MutationWorkspace whole-file writes and exact replacements. Two legitimate pipe-numbered prose cases were falsely rejected. No arbitrary patch API was exercised. **Multi-file changes are sequential, not atomic.** A valid earlier change can remain when a later operation is rejected.
- No-progress exceptions depend on trusted classifications. HR was reconstructed, not raw-replayed. Scripted physical recovery is not autonomous model recovery. Slow generation is distinct from repeated completed tool results.
- Raw model qualification events retain a recipe-ID/Job-ID attribution mismatch, reconciled externally without rewriting originals. Canonical usage accounting remains UNATTESTED; reported resource totals derive from actual provider responses.
- The synthetic forensic vault is gated and not approved for private production capture. Permission, revocation, retention and all-secret redaction are not qualified.
- All original 98 scanner candidates were reviewed; four findings correspond to two fixed renderer defects. The native ledger still has 98 needs-validation records with separate manual dispositions. **12/12 coverage units remain PARTIAL.** No complete-security or remote exploitability claim follows.
- Final fault delivery matched 17/18 frozen assessor expectations. The remaining timeout selected TRANSPORT_TIMEOUT rather than MODEL_CALL_DEADLINE_EXCEEDED and failed closed. Earlier invalid setup attempts and assessor mismatches remain evidence.
- Two pristine installs and installed-service upgrade/rollback used the exact recorded runtime revision. Later changes are dashboard encoding and test/documentation only. New experimental event/config downgrade compatibility remains unqualified; retain pre-upgrade backups.
- Single VM startup and post-Job RSS samples are not statistical/idle performance qualification. Per-event/tool/tracker overhead, long soak and measured energy remain unavailable.
- Identity has twelve stable colour slots, not unlimited unique colours. Explicit identifiers/status labels remain necessary.
- Live videos are unsigned, capture bounded scenarios, and distinguish controlled responses from real inference. Real model recording has two overlapping parts with a disclosed contrast correction; it is not a single continuous polished recording.
- AutomationBench and Strands adapters are EXCLUDED. No external benchmark score, AWS API-equivalent local-inference cost, universal model parity or universal Linux support is claimed.

No merge, tag, publication, deployment or upgrade of an existing installation is part of this RC qualification.

The source archive's public-distribution privacy gate remains BLOCKED: 1,102 inherited host/path matches require disposition or sanitized packaging. The three credential-shaped and four private-IP occurrences are explicit synthetic security fixtures. Zero new matches does not clear inherited publication risk.
