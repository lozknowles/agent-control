{
  "name": "agent-control",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Multi-lane terminal mission control for local AI agents",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "test": "node --test --import tsx src/context.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "blessed": "^0.1.81"
  },
  "devDependencies": {
    "@types/blessed": "^0.1.25",
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
