{
  "name": "agent-control",
  "version": "3.0.1",
  "private": true,
  "type": "module",
  "description": "Infrastructure-neutral policy control plane and adaptive agent harness",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "test": "node --import tsx --test src/*.test.ts src/control/*.test.ts src/ui/*.test.ts scripts/*.test.mjs",
    "typecheck": "tsc --noEmit",
    "check:bootstrap": "node --check scripts/control-plane.mjs scripts/config.mjs scripts/qualify-all.mjs && bash -n android/resource-agent.sh android/start-node.sh android/install-boot.sh android/termux-boot-agent-control.sh scripts/qualify-remote.sh",
    "check:neutrality": "node --test scripts/infrastructure-neutrality.test.mjs",
    "check": "npm run typecheck && npm run check:bootstrap && npm run check:neutrality && npm test",
    "qualify": "node scripts/qualify-all.mjs",
    "qualify:all": "npm run qualify",
    "up": "node scripts/control-plane.mjs up",
    "status": "node scripts/control-plane.mjs status",
    "down": "node scripts/control-plane.mjs down",
    "provision:android": "node --import tsx scripts/provision-android.mjs",
    "demo:context": "node --import tsx scripts/demo-context-consensus.mjs",
    "qualify:openai-chatkit": "node --env-file=.env.local --import tsx scripts/qualify-openai-chatkit.mjs"
  },
  "dependencies": {
    "blessed": "^0.1.81"
  },
  "devDependencies": {
    "@types/blessed": "^0.1.25",
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
