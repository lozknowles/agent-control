# Dashboard behaviour

Job details now render canonical per-Run invocation history. Active rows show invocation ID, lane, step, provider, model, state, phase, start time and elapsed time; usage and cost say `awaiting completion`. Completed rows show token attribution, reported/estimated/unknown source, duration, outcome and verification result.

Job totals continue to aggregate many invocation records; individual rows retain exact attribution.
