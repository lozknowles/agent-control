# Next steps

- Run the frozen Luna → qualified local LLM → qualified GLM-5.3-Flash → Luna context-chain experiment when all three model-registry routes are available on one isolated qualification node.
- Add stdio/WebSocket ACP host packaging and run external ACP client conformance tests; keep the current adapter core authoritative mapping unchanged.
- Repeat the fast-execution benchmark across larger real repositories, multiple Codex client versions and accounts; collect provider cost before considering default enablement.
- Extend session lifecycle mutation APIs only after defining external authentication and participant-admission policy; the current dashboard remains read-only for sessions.
